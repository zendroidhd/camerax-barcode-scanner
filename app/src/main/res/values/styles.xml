<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black_color</item>
        <item name="colorPrimaryDark">@color/black_color</item>
        <item name="colorAccent">@color/arcade_red</item>
        <item name="android:navigationBarColor">@color/black_color</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowActivityTransitions">true</item>

    </style>
    
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    
    <style name="TextInputLayoutAppearance" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Hint color and label color in FALSE state -->
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorPrimary">@color/white</item>
    </style>
    
    <style name="ArcadeTextView" parent="Widget.AppCompat.TextView">
        <item name="android:fontFamily">@font/arcade_font</item>
    </style>
    
    <style name="IconOnlyButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="iconPadding">0dp</item>
        <item name="iconGravity">textStart</item>
    </style>
    
    <style name="AppTheme.ClearBackGroundWhiteRipple">
        <item name="colorControlHighlight">@color/super_transparent_white</item>
    </style>
    
    <style name="TextInputStyle" parent="Widget.Design.TextInputLayout">
        <item name="colorControlActivated">@color/transparent_white</item>
    </style>

</resources>
